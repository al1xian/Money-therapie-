/**
 * The storefront's currency, in one place.
 *
 * Shopify prices this shop in euros; nothing here can change that, so this is
 * a label rather than a setting. Stated once so the header, the footer and the
 * menu cannot drift apart on it.
 */
export const CURRENCY_CODE = 'EUR';
export const CURRENCY_SYMBOL = '€';
